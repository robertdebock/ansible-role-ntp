---
# vars file for ntp
ntp_configuration:
  Alpine:
    file: /etc/ntpd.conf
    packages:
      - openntpd
    service: openntpd
  Debian:
    service: ntp
  default:
    file: /etc/ntp.conf
    packages:
      - ntp
    service: ntpd

ntp_packages:
  Alpine:
    - openntpd
  default:
    - ntp

ntp_services:
  Archlinux:
    - ntpd
  Alpine:
    - openntpd
  CentOS:
    - ntpd
  Debian:
    - ntp
  Fedora:
    - ntpd
  openSUSE Leap:
    - ntpd
  openSUSE Tumbleweed:
    - ntpd
  Ubuntu:
    - ntp

ntp_configuration:
  Alpine: /etc/ntpd.conf
  default: /etc/ntp.conf
