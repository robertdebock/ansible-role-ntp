---
# vars file for ntp
ntp_packages:
  Alpine:
    - openntpd
  default:
    - ntp

ntp_services:
  Archlinux:
    - ntpd
  Alpine:
    - openntpd
  CentOS:
    - ntpd
  Debian:
    - ntp
  Fedora:
    - ntpd
  openSUSE Leap:
    - ntpd
  openSUSE Tumbleweed:
    - ntpd
  Ubuntu:
    - ntp

ntp_configuration:
  Alpine: /etc/ntpd.conf
  default: /etc/ntp.conf
