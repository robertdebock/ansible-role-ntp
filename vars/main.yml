---
# vars file for ntp
ntp_packages:
  Alpine:
    - openntpd
  default:
    - ntp

ntp_services:
  Archlinux:
    - ntpd
  Alpine:
    - openntpd
  CentOS:
    - ntpd
  Debian:
    - ntp
  openSUSE Leap:
    - ntpd
  Ubuntu:
    - ntp

ntp_configuration:
  Alpine: /etc/ntpd.conf
  default: /etc/ntp.conf
