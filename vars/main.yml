---
# vars file for ntp
ntp_packages:
  Alpine:
    - openntpd
  default:
    - ntp

ntp_services:
  Alpine:
    - openntpd
  default:
    - ntp

ntp_configuration:
  Alpine: /etc/ntpd.conf
  default: /etc/ntp.conf
